extends "res://ui/CSS/CharacterSelect.gd"

func _ready():
	print("Adding characters...")
	# This is the script which adds the characters to the game
	# The tool will automatically add any characters you make using the tool
	# but just in case here's the template code:
	# addCustomChar("the name of your character", "the scene where your character is")
